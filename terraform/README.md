# Terraform platform contract

Reusable modules should encode secure defaults, remote state + locking, ownership / cost tags, encryption, network conventions, backup controls, observability hooks, and predictable outputs.

Module consumers should not need to rediscover production safety requirements.
