# Kubernetes / ECS platform contract

Standardize resource sizing, autoscaling, disruption budgets, topology spread, workload identity, network policy, graceful shutdown, immutable images, release strategy and tenant isolation.
